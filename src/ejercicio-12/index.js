// Escribe tu código aquí:
// Scope de Bloque: Utiliza let para declarar una variable dentro de un bloque (if, for, etc.). Intenta acceder a esa variable fuera del bloque y observa si puedes acceder a ella.

for (let i=0;i=10;i++){
    let variable = 2
}
suma = variable + 2
console.log(suma)

